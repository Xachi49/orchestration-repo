# Read-only GitHub token for GitHubReadOnlyAdapter.
# Minimum privilege (contents:read / public repo read). Never commit a real token.
# The local fake stack (npm start) does not require this variable.
GITHUB_TOKEN=
